
ReadMe anlegen
    wie werden daten importiert
    werden schlüssel benötigt (auth)
    wo werden sie abgelegt
    naming
    wass muss im code angepasst werden
        oder wird es beim start vom program abgefragt
        kann phyton auf daten ausserhalb vom program zugreifen?
        ist das überhaupt gewünscht?